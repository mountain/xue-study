# 2026-09-24 有类型球面谱、周期理论与年周期实验

用户要求把向量推广成球上谱、推进多物理量表示、研究 Adva 中李天岩与 Sharkovsky，
并提出联合时空谱以及“年周期加周期扰动”。新增 `experiments/typed-spectrum/`，
理论与边界集中于 `docs/sphaera-spacetime-and-adva-periods.md`。

空间原型含局部向量/对称张量反演、全球标量与切向风基底、奇偶核、带域掩膜的
加权拟合以及给定风的标量平流。空间单测 11 项通过，合成刚体平流最大误差 1.71e-12。
真实单时次 ERA5 拟合 12 标量及 850/500/250 hPa 三风层；仅表达大尺度，未训练多变量预报。
SST 受限域拟合 RMSE 1.436 K，错误的陆地绝对温度填零对照 68.895 K。

`temporal.py` 保留双侧复频率、相位与窗归一化功率，拒绝不规则/缺测时间。
刚体旋转的三个空间模态验证 ω=mΩ，而非 ω=ℓΩ。
精确有理矩阵给出纯 ℓ=2、三步周期、保持距离的反例：空间奇偶不能等同时间周期强迫。

独立冻结的 `annual-contract.json` 限定 384 个合成月样本、无参数搜索。
四年相加扰动峰为 0.25 次/年；调幅产生 0.75 与 1.25 次/年；调相在小幅时有相近功率、
不同复相位及高阶项。只用训练年份估计季节基线，不受留出值污染。
一个周期系数标量 ODE 验证年度 Floquet 倍率 exp(−0.25)。没有识别真实气候周期。

Adva 只读复核钉在 1ecd29b47d216b4e1c482d61a1c3e68252f8a07b。
0167、修订的 0168、商映射纠正、区间延拓与 observer quotient 记录已读。
三个既有外部有限实验重放通过；Rust `observer_quotient_descent` 7/7 通过。
最初 cargo 路径不存在，尚未运行测试即退出；改用 `/usr/bin/cargo` 后成功。
矩阵记录与原冻结证据仅时钟、资源上限及 RSS 不同，数学载荷相同。
Adva 工作树保持干净，未创建新的原生语义声明、Seal 或气象学习器。

用户随后提出使用 Observable @mountain。另建 `experiments/observable-seasonal/`，
采用当前 Notebook Kit 2.6.4 的 9 单元开放笔记本格式，配合可复制单元与 JSON 附件。
账号连接目录无匹配插件；匿名网页被浏览器验证页阻断。没有登录、上传、发布或声称发布。
已授权的已有 Xue 预报和站点均未更改；这些新增页面和笔记本仍是本地研究产物。

收尾核查：Notebook Kit 实际本地构建成功；首稿手机长公式产生横向溢出，拆成两行，
发布候选改存 `archive/observable-seasonal-v2/`，v1 保留。测试脚本另修正两个断言：
空表显示“No results.”而非零行；不得把字段名 seasonalFitError 中的 Error 当运行错误。
年度独立 HTML 页面各控件、零扰动、手机宽度与无 JS 异常均通过。

纪律检查第一次误用无 numpy 的系统 Python，且较早基础变量维护说明的首 14 行
缺少检查器能识别的单文件路径。补一条真实 README 路径，改用已有数值环境，
并为硬编码的 `/tmp/xue-upstream` 建立指向现有 `/home/ubuntu/xue` 的符号链接后，
methodology_check 输出 7/7；卡片验证器依旧跳过，不是卡片语义已通过。diff 空白检查通过。
