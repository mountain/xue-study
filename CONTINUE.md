# 继续这份工作

写给**下一个接手的人或 agent**。目标不是复述做过什么，而是让你在读完之后
**能直接干活、并且不去重走已经走过的死路**。

---

## 一、先读这一节：诚实的期望

**这份工作的目标**：青藏高原的冰川垮塌与冰湖溃决风险分析。

**已经建立的、你必须先接受的三个结论**（都来自本仓库的实测，不是意见）：

1. **这类事件的精确率被基率封顶**（`xue.derived.discrimination-ceiling.v0`）。
   精确率 = p/a，其中 p 是基率、a 是报警率。**大气物理不进入这一行。**
   我们已经实测的三个探测器报警率都在基率的数千倍上 —— 误报是算术后果，
   不是特征的毛病。**所以「找一个更好的特征」不会修好它。**

2. **三个探测器中，两个是结构性失明的，不是精度不够**：
   - ITS_LIVE 测**冰面流速**，对**岩体**前兆失明（本次事件的前兆在岩体）
   - PDD 触发器 **91% 的时间都触发** —— 饱和即无分辨力
   - 机场测站在 2000 m 以上**只有 2 对**，所以高海拔代表性误差**量不到**

   > **共同点：限制不在数据量，而在观测所在的位置。**
   > **而位置由观测系统当初的目的决定，不由我们的问题决定。**

3. **因此，不要把「再找一个数据集」当作默认解法。** 先问：
   **那个观测系统当初为什么被部署在那里？** 答案会告诉你它能不能回答我们的问题。

**这份工作能救人的方式，很可能不是「预报垮塌」**，而是**审计别人用来预报的仪器**。
已经有两项这样的产出（见第四节），它们是可以直接给别人用的。

---

## 二、机器怎么跑起来

**这是最要紧的一节。** 团块的采集一旦停止，丢掉的观测**永久补不回来** ——
上游保留极短（实测：`cma` 雷达只有约 2.4 小时；`gfs` 自述 "only the newest is kept"）。

### 采集器（必须常驻）

```sh
cd /Users/mingli/Climate/xue-study
python3 scripts/xue_collect.py --loop 600 --max-mb 40
```

- 每 10 分钟一轮，**perishable 源优先**（cma/mrms/jma/sounding/airport）
- 归档到 `archive/`，索引 append-only，**策略性跳过与出错跳过分开标注**
- 若要开机自启，需用户自己装 LaunchAgent（`~/Library/LaunchAgents/` 在本工作区沙箱外）

### 团块页面

```sh
cd /tmp/xue-upstream && npm_config_cache=/tmp/npm-cache npm run dev -- --host :: --port 5199
cd /Users/mingli/Climate/xue-study && while true; do python3 scripts/block_page.py; sleep 120; done
```

页面在 **http://127.0.0.1:5199/block.html**，三个区块：清单 / 技巧表 / 归档的场。
`--host ::` 是必须的：用 `--host localhost` 在 macOS 上只解析到 `::1`，`127.0.0.1` 打不开。

### 依赖

系统 python3 **没有 numpy**。需要 numpy/xarray/zarr 的脚本要跑：

```sh
export UV_CACHE_DIR=/tmp/uv-cache          # 沙箱不允许写 ~/.cache/uv
uv run --offline --with numpy python scripts/xxx.py
# 或用一个已装好的环境：/tmp/m2env/bin/python（含 numpy/scipy/xarray/zarr/s3fs/geopandas）
```

---

## 三、已经确认的死路（**不要去重试**）

| 死路 | 证据 |
|---|---|
| Harmony 子集服务 | 主机级阶段性阻断（根路径 000），非 URL 形状问题 —— 8 次尝试、变量数 1/2/3、GET/POST 全重置 |
| OPeNDAP 取 MERRA-2 | 已退役（`.dds` 返回 `410 Gone`） |
| MERRA-2 日统计产品答 850 hPa 问题 | 只有 8 个变量，**全是地面量**，含 "850" 的变量 0 个 |
| 用卫星振幅找本次事件前兆 | 检测限约 **1 dB / 两月**，而前兆是 **~10 mm/月**，低一个数量级 |
| 用 ITS_LIVE 找前兆 | 已验证：2021–2023 该处流速是基线的 **0.67–0.73**，是**变慢** |
| 从 ITS_LIVE 影像对取季节基线 | `date_dt` 中位 **264 天**；选不选短对会让季节形态**翻转**（峰 3 月 vs 峰 10 月） |
| Zenodo / archive.org / eos.org / NASA COOLR | 本会话内阶段性不可达或 403 |
| GLIMS 页面的 RGI 下载 | 页面**没有直接下载链接**，只有 DOI 与 Zenodo |
| 把 Zarr 存储当文件 GET | 必然 404。键名是 `<节点>/c/<索引…>`，`c` 是独立路径段 |
| 把 poster 当普通栅格读 | poster 是**垂直差分编码**的，须 `cumsum(axis=0) % 256` 反滤波，否则画出的是差分 |

---

## 四、可以交给别人的产出（**最可能真正救人**）

### 1. 高原 850 hPa θe 是**约定，不是观测**

三个再分析系统对同一个问题的三种做法：

```
ERA5      ~331.6 K（63 年九月中位）   给值，贴着地面
GFS/xue   366.2–371.3 K              给值，按递减率外推
MERRA-2   屏蔽 65.1%                  拒绝作答（屏蔽位置与「PS<850」吻合 97.3%）
```

地形三家一致（579.7 / 582.6 / 582.9 hPa），暖池三家一致（差 <1.4 K）。
**所以分歧不在物理，在对一个观测上没有定义的量各自选定的约定。**

**任何建立在高原 850 hPa 上的风险指数都继承这个约定。** 这是可以直接引用的结论。

### 2. 判决取决于误差标准

同一次比对、同一个观测，一致性可以是 **36.2% 或 94.5%**，取决于把什么算作误差。
实测分解（总量 2.14 K）：

```
代表性   0.96 K（下限）   20% 的方差
仪器     0.50 K            5%
模式     1.85 K           75%
```

**任何运行阈值都必须写明它用的是哪一个误差标准**，否则阈值不可比。

---

## 五、还没做、且**不依赖外部数据**的（接手后从这里开始）

1. **非事件侧与特征表**（`(1b)` 的剩余部分）。
   事件侧已通：**事件 = 带不确定度的观测进入系统**（`docs/event-stream.jsonl`，32,222 条，
   误差必填且来源具名）。非事件侧仍缺一个能定义「一条冰川」的边界数据。
   **绕开 RGI 的办法**：用 ITS_LIVE 自己的 `landice` 掩膜做连通域（本仓库已做过，
   朗唐邻域 564 像元 → 3–4 个连通体），**标明它是代理而非权威边界**。

2. **接入更多带误差的观测源**：sounding、cma、mrms 尚未进事件流，
   它们各有可计算或可声明的误差（例如雷达拼图的空间覆盖本身就是个可量化的量）。

3. **把团块里的运行数攒起来**：目前只有 **1 次** gfs 运行，
   所以「跨运行汇总技巧曲线」**并没有真的发生**（曲线变宽靠的是观测累积）。
   等运行多了再做，并且**不要把「样本变多」记成「方法变好」**。

4. **审查 `archive/` 的存储增长**：现有策略约 30 MB/轮。
   若长期跑，需要估算配额并调整 `--max-mb` 与 `FULL_COLLECTIONS`。

---

## 六、这个仓库的纪律（**不要破坏**）

- `docs/claims.toml`：**每条 claim 必须 11 个字段**
  （`claim_id` `canonical_name` `code_symbol` `dimension` `status` `scope`
  `assumptions` `dependencies` `proof_or_certificate` `counterexample_boundary`
  `forbidden_conflations`）。写入前**必须用 `tomllib` 校验**，
  写坏了本会话发生过三次。改动后跑 `scripts/repin_contract.py`。
- `docs/xue-verification-report.md` 与 `docs/maintenance/*`：**append-only**。
  发现前面的结论错了，**追加一条更正**，不要静默改掉。
- `scripts/methodology_check.py`：7 项自检，改动后必须仍 7/7。
- **区分 `RECORDED` 与 `OBSERVED HERE`**：从论文读来的数不是本仓库的测量。
- **区分三类不确定度**：`declared` / `computed` / `observed-scatter`（受污染，混了模式与代表性误差）。
- **区分「未建立」与「无技巧」**：样本不足不是结论。

### 本会话反复踩的坑，写在这里省你时间

1. **读取器已经解码了，不要再套一遍。** GRIB（GDAL 返回已是 °C，又减 273.15）
   和 Zarr（xarray 已应用 `scale_factor`/`add_offset`，又套一遍）各栽一次，
   症状都是「结果差一个巨大常数」。**变换前先看 `.encoding` / `GRIB_UNIT`。**
2. **「结果像噪声」时，先怀疑「我读的是不是那个量」**，再怀疑布局和配色。
   poster 那次就是画的差分而非值。
3. **两侧返回完全相同的字节数 = 这个检查不会失败。** Vite 的 SPA 回退页
   让已删除的路径返回 200 且字节数恒为 57912 —— 本会话被它骗过两次。
4. **进程表不可靠**：`pgrep`/`ps | grep` 会匹配到包装命令，
   给出假的「在跑」或「没跑」。用受管任务的作业列表。
5. **一个不会失败的控制不是控制。** 平坦的曲线同时兼容两个相反的机制；
   三国再分析互比发现不了它们共享的原料误差。

---

## 七、2026-09-23 补充：热穹顶与「退出方案」

### 已完成并提交：高原热穹顶在**地面之上**重做（`d05488c`）

**先解决仪器问题**（本仓库实测：高原地面气压中位 582.6 hPa）：

```
850 hPa   在地下（67.9% 的格点）  ← 「热穹顶」是地下外推值削平到编码上限的产物
500 hPa   在地面之上              ← 可用
250 hPa   归档中最高可用层次（xue 无 200）
```

**结果 —— 跨模式分歧在 500 hPa 上消失：**

```
500 hPa 温度 °C      高原      暖池      中欧
aifs                -4.2     -4.2    -14.3
ecmwf               -3.5     -4.0    -14.0
gfs                 -3.8     -4.7    -14.3
跨模式极差           2.0 K    1.0 K    1.5 K
```

> **高原不是模式天然分歧的地方。只要问一个确实存在的层次，三家就一致。**
> 850 hPa 的 35–40 K 分歧完全来自「向一个在地下的层次要值」。

**而真信号在**：500 hPa 上高原（−3.8～−4.2 °C）与暖池（−4.0～−4.7 °C）几乎一样暖，
中欧同层次 −14 °C。**这是真实的抬升热源信号，不需要任何假象就能看到。**

### 断点：退出方案未完成（**`scripts/sah_exit.py`**）

研究的是**南亚高压**（夏季盘踞高原的行星尺度反气旋），经典层次 100–200 hPa。

**已跑通的部分：**

```
等值线   11316 gpm（按实测 p75 定）
事件     111 次，「覆盖 ≥50% 且持续 ≥3 天」，1979–2021 的 JJA 每日 00Z
持续     中位 5 天，最长 25 天
```

**坏掉的部分：退出分类全部 `undefined`。**

- 症状：`dpos` 非有限 → 质心 `clat`/`clon` 为 NaN
- **未定位**。`above` / `wsum` / `clat` 三行读起来是对的
- **建议的下一步**（不要再读代码猜）：在质心算完之后，直接
  `print(clat[a:b+1], clon[a:b+1])` 打出前三个事件，**看是哪一天变 NaN**
- 也可能是 `np.percentile` 遇 NaN：先试 `np.nanpercentile`

**环境：**

```sh
export UV_CACHE_DIR=/tmp/uv-cache        # /tmp/m2env 与 uv 缓存都已被清过，需联网重取
uv run --quiet --with numpy --with xarray --with zarr --with fsspec --with aiohttp \
  python scripts/sah_exit.py --first 1979 --last 2021 --percentile 75 --json /tmp/sah.json
```

**ERA5 源**（匿名，无需凭据）：

```
https://storage.googleapis.com/gcp-public-data-arco-era5/ar/
  1959-2022-6h-240x121_equiangular_with_poles_conservative.zarr
层次 50 100 150 200 250 300 400 500 600 700 850 925 1000   ← 100 与 200 都在
时间 1959-01-02 .. 2021-12-31，6 小时；经度 0–360
```

**注意读取会超时**：分块是 8 个时次 × 全层次 × 全空间（约 1.2 MB），
读 JJA 每日 00Z 约 600 MB。慢，且会断。必要时分年跑、加 `--retry`。

### 新增的死路（**不要重试**）

| 死路 | 证据 |
|---|---|
| 用文献的 12500 gpm 等值线定 SAH | **该等值线从未进入高原框**（实测最大 11974 gpm）。结果是「零事件」，看起来像「不存在这种事件」，实际是阈值错 |
| 用 `(lon>=w) \| (lon<=e)` 做经度掩膜 | 这是**并集**，任何经度都满足。只在框**跨越 0/360 接缝**时才该用。第一版选到全部 240 格 |

### 新增的两条教训

1. **守卫只挡一个方向等于没有守卫。** 掩膜守卫只拒绝了「选了零格」，
   **没拒绝「选了全部格」** —— 而后者正是那次实际的 bug。
   **任何「选择」都要同时设上下界，并打印选取比例。**
2. **`str.replace` 必须带断言。** 一次替换因为引号不同（`'--json'` vs `"--json"`）
   **静默什么都没做**，直到程序报 `unrecognized arguments` 才暴露。
   同一段代码里第一处替换有断言就报了错，第二处没有就无声失败。

### 第三次同一类解码错误

脚本写 `q.get("unit","K")`，但 `q` 是**量化字典**，**没有 `unit` 键**，
于是默认成 K，对摄氏值又减 273.15，得出 −273 °C。

**单位在 *variable* 字典里，不在 *quantization* 里。**

> 与 GRIB（GDAL 返回已是 °C，又减 273.15）、Zarr（xarray 已解码，又套量化）同类。
> **判据：数值荒谬（−273、1e8、−71）时，先查单位与解码，不要查物理。**
