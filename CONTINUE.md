# 继续这份工作

> **2026-09-24 用户对数据真实性的保留意见（最新）**：用户明确要求将其怀疑加入三地
> 遥相关报告。已加入 `experiments/teleconnection-audit/README.md` 开头及剩余问题。
> 不将怀疑擅自限定为某个数据环节；哈希、同源读回和复现不消除此疑虑。
> 现有数字是依赖输入与假设的条件性结果，未证实造假，也未完成独立真实性核验。
> 冻结证据不改写；新附录和带保留意见的报告副本存于原审计归档，单独记录哈希。

> **2026-09-24 三地遥相关复核（最新）**：用户要求遵守 Adva 纪律，追溯背景风来源，
> 并确认东亚近海、北美东岸、中东 500 hPa 三处同步。已完成
> `experiments/teleconnection-audit/README.md`，看结果前冻结 `contract.json`；
> 结果/CSV/校准/完整模拟/图在 `archive/teleconnection-audit-v1/`。
> NOAA 原始 u/v NetCDF SHA 与凭据匹配，500 hPa 独立读回 NPZ 完全一致。
> 背景由我们对 1979—2014 同月平均再投影计算，不能说 NOAA 将该现象判断为噪音。
> 三地 1979—2025 月区域风速原始 r=0.847/0.928/0.850；扣月均后 −0.061/0.101/0.044。
> 次级方向线索：东亚—中东 v 距平 r=−0.231，冬季 −0.384，不能据标量风速排除波列联系。
> 六张 L12 预报本身同步强，但都含同一构造的月气候态，不能作为独立遥相关证据。
> 季节结构不是噪音；内部数值分解、再分析统计关联、机制归因需分开。
> 没有确认或否定全部遥相关，也没有 ENSO 因果结论；下一轮可固定联合风/位势高度距平
> 模态与相位及时滞，并用独立来源复核。未改变已发布数值或重启训练。

> **2026-09-24 局地异常与日期核对（最新）**：用户指出北半球三处风变和高原温度异常，
> 随后明确关心 2026 年 12 月厄尔尼诺，以为现有预报已覆盖。已实际读取公网四模型
> manifest 和 Zarr 时间数组：全部仍为 2025-12 初始、2026-01—06 六个月历史实验。
> 生成日 2026-09-24 不是实时起报日；当前没有 2026-12 预报文件，不得声称已经覆盖。
> NOAA 2026-09-10 展望确实给出 2026—27 秋冬极强厄尔尼诺概率 >90%，但属于外部预测。
> 新诊断 `experiments/multivariate-refinement/diagnose_regional.py`，归档
> `archive/regional-audit-20260924/`，说明 `docs/maintenance/2026-09-24-regional-forecast-audit.md`。
> 高原框 28—36N/80—100E 一月 L12 相对缓存 2026 再分析暖 +10.43°C，其中训练季节背景
> 谱截断 +9.59°C、学习距平 +0.62°C；是表示误差，不能归因厄尔尼诺。变量为 2 米气温。
> 诊断性保留原生训练气候态、只叠加谱距平后均值偏差约 +0.845°C，但尚未全面验证或上线。
> 北半球三处一月风速峰也存在于原生训练气候态；东亚一月至六月矢量变化 29.0 m/s，
> 谱气候态已有 27.3 m/s，不能说是新学到的异常或 ENSO 归因。前轮验收漏了局地质量检查。
> 本轮未改写线上科学数据。下一步应修复地形季节背景并取得最新完整月份重新起报，
> 保留粗细旧档，再检验十二月 ENSO 相关距平；当前尚未执行这些修复和重新起报。

> **2026-09-24 空间加密 L12 与粗档保留（最新）**：用户要求按比例加细且粗网格仍可调用。
> 已完成 L6→L12 谱训练和 2.5°→1.25° 输出加密；月均、16 变量、原始资料和时间划分不变。
> 新实验 `experiments/multivariate-refinement/README.md`；产物 `archive/multivariate-l12-v1/`。
> 新模型 `ctmulti12`，2698 系数，验证选 rank=64 / penalty=0.1。原始 `ctmulti` L6 完整保留，
> 开始前冻结的 140 个粗档对象 SHA 全部不变。L12 粗网格取样另存，不冒充原始 L6。
> 入口 `/resolution.html`，脚本/Observable 目录 `/research/resolutions.json`；首页默认细档，
> 模型菜单仍可选粗档，切换保持变量、月份、视角及播放状态。旧 FULL/EVEN 也保留。
> 完整原生域物理 MSE 较 L6 降 36.46%，气候态本身降 35.70%；主要是季节背景表示收益，
> 不能声称异常预报能力提高 36%。2020—2025 是已暴露开发回测，非全新独立检验。
> Gram 去重节省约 2.62 GiB；投影嵌套残差、模型重放、原生格点误差独立核对均通过。
> SST 仍有低温越界，仅研究下载；细档 tmp2m 自描述编码范围 −80—47°C，旧档未改。
> 已发布 `/var/www/climatetensor/releases/multivariate-l12-v1-20260924`，`current` 已切换，
> 旧 release 保留。公网检查和最终状态见新归档 `implementation-verification.json`。
> 下一阶段先约定日/周资料和验证，再考虑 L24；未做时间加密、同化或实时业务预报。
> 不覆盖任何已经发布的科学版本，不把工程联通通过当预报能力证明。

> **2026-09-24 多变量联合训练（最新）**：用户要求加入多变量并逐渐提高时空分辨率。
> 已完成 NCEP/NCAR R1 + ERSST v5 的 1979—2025 月历史下载、16 变量 / 13 通道 /
> 778 系数 L6 表示与联合 EOF/ridge 训练，见 `experiments/multivariate-seasonal/README.md`。
> 训练 1979—2014，验证 2015—2019 选 rank=32、penalty=0.1；2020—2025 明确为已暴露的
> 开发回测。联合整体较气候态好，但较独立通道误差高 5.39%；500 hPa 一月技巧 6.60%。
> 不能声称联合模型总体胜出。SST 低阶重建有低温越界，单列 QC，仅研究下载。
> 产物在 `archive/multivariate-seasonal-v1/`；Xue 新模型 `ctmulti`，11 图层，14 分量；
> 完整下载有 16 变量，sp/SST 不作为本版地图层。页面 `/multivariate.html`。
> 初始仍为 2025-12，目标 2026-01—06，生成 2026-09-24，非实时、未同化。
> 本轮未增加分辨率；后续按月均 L6→L12、再日/周资料、再 L24，逐阶段检验。
> 已发布到 `/var/www/climatetensor/releases/multivariate-v1-20260924`，`current` 已切换。
> HTTPS 140 个对象与本地一致，三模型指针 CRC、Range 206 和研究下载 CORS 已通过。
> 浏览器分批证据见 archive 中 `browser-local-*.json` / `browser-public-*.json`；
> 最终线上 11 图层、默认首页、三模型切换、点位 6/6 和手机布局均通过，零网页/HTTP 错误。
> 汇总为 `implementation-verification.json`；临时预览已停止，公开 Caddy 保持 active。
> 不覆盖第一版或这批已冻结产物，不把交付检查当预测能力证明。

> **2026-09-24 Observable 首篇验收完成（最新）**：用户已贴出线上校验表，四项值为
> 384 / true / 8.882e-16 / 0.7788007831，与本地附件按显示精度逐项一致。
> 结合先前用户确认控件和两张图正常，以及公开版本 43 的源码核对，本轮发布检查完成。
> 运行结果证据来自用户浏览器反馈，助手网页自动访问仍受限。记录为
> `archive/observable-public-review-20260924/user-runtime-confirmation.json`。
> 本篇是合成年周期与扰动实验；没有新增真实气象模型训练或技巧声明。

> **2026-09-24 线上校验单元已替换（最新）**：用户更新后的公开版本为 43。
> 已从公开 API 确认第 8 单元（线上 id 11）直接读取 annual-check.json，显示四行表格，
> 并捕获读取错误；错误的 calibration-cell.js 导入已不存在。其余 8 单元与版本 27 一致。
> 用户已确认图表与下拉框正常；实际在线附件数值仍未由助手观察到。
> 记录：`archive/observable-public-review-20260924/review-v43.json`。本地完整 v3 是另一种
> 等效的状态面板修订，不应声称线上逐字采用 v3；线上采用的是随后给用户的简明表格代码。

> **2026-09-24 校验显示修订（最新）**：用户确认 Observable 下拉框与两张图正常，
> 但没有看到 samples/trainingOnlyFit。原第 8 单元使用折叠对象显示，实际附件结果
> 尚未确认，不能直接判断为附件缺失。本地 v3 将该单元改成读取状态、直接可见表格和
> 明确错误；整页桌面/手机及正常/缺失/网络/JSON/字段错误控制通过。
> 只需替换 `experiments/observable-seasonal/calibration-cell.js` 的单元内容。
> v3 尚未写入线上；当前公开版本仍按 v2 核对。下载包在 `archive/observable-seasonal-v3/`。

> **2026-09-24 Observable 已发布（最新）**：用户已发布
> https://observablehq.com/@climatetensor/annual-spectrum-1 。公开文档 API 确认
> public 版本 27，创建者 mountain、所有者 climatetensor，9 个单元与本地 v2 完全一致
> （忽略首尾空白）。新版 `annual-check.json` 附件解析引用存在；网页自动浏览返回
> Vercel 验证页，尚未确认在线附件和交互运行。已向用户询问底部是否有
> samples: 384 / trainingOnlyFit: true。核查记录在
> `archive/observable-public-review-20260924/review.json`；没有账号写入权限。
> 下方“尚未发布”段落保留为较早状态。

> **2026-09-24 时空谱与 Observable 恢复点（新增）**：已推进标量/切向风/局部张量的统一
> 方向响应原型 `experiments/typed-spectrum/README.md`；11 项空间检查、真实单时次
> 12 标量＋3 风层拟合、合成平流及交互页通过。时空谱核对 ω=mΩ，年周期实验区分
> 独立周期、调幅、调相，保留复相位。详见 `docs/sphaera-spacetime-and-adva-periods.md`。
> Adva 0167/0168 与纠正链已复核；3 个外部有限实验及原生商观测 Rust 7 项通过，
> 未修改 Adva、未证明通用混沌或原生气象学习。新实验没有多变量时间训练或同化。
> 用户进一步提出发布到 Observable `@mountain`。首篇本地笔记本位于
> `experiments/observable-seasonal/`，针对 Notebooks 2.0；没有可用账号连接，
> 尚未写入或发布到 Observable。依照该目录 README 查看本地构建与迁移包。

> **2026-09-24 基础变量恢复点（新增）**：用户提出 16 项变量并授权调整。
> 新配置位于 `experiments/basic-state/README.md`、`variables.py`：保留原量，
> 补 sp/msl/u500/v500/q850/q700/ssr，共 23 时变量及 lsm/zs 两个静态辅助场。
> 已完成双 ARCO 源元数据核查和 2020-01-01 单时次实读（21 时变量 + 2 静态）；
> RH 在两个镜像均缺，已列入未提交的 CDS 月均请求计划。原始样本在仓库外
> `/home/ubuntu/climatetensor-inputs/era5/basic-state-smoke-20200101/`。
> 4 项变量检查通过；本轮没有多变量训练、同化或新变量上线。
> 详见 `docs/maintenance/2026-09-24-basic-state-variables.md`，不得把配置/取样称为新预报。

> **2026-09-24 发布恢复点（新增）**：已按用户选择开始 500 hPa 风场谱学习首版。
> 阅读 `experiments/wind500-seasonal/README.md`。数值原型已完成真实历史月均资料训练、
> 1979—2014 拟合 / 2015—2019 选参 / 2020—2025 留出检验。它不是 Adva 原生学习器。
> 公开月均源目前到 2026-02，因此首批固定为 2025-12 初始、2026-01—06 有效的历史起报实验；
> 实际生成于 2026-09-24，不是当前业务预报。完整谱一月低阶 MSE 改善约 9.05%，
> 三至六月增益很小。两组都保留完整月气候态，even 仅限制异常的通道。
> Xue 独立发布工作树 `/home/ubuntu/climatetensor-xue`；证据在
> `archive/wind500-seasonal-v1/`。HTTPS 服务由 Caddy 管理。
> 已公开于 https://climatetensor.io/?lang=zh ，方法、检验与下载位于 `/experiment.html`。
> 服务目录 `/var/www/climatetensor/current` 指向 `releases/wind500-v1-20260924`。
> 用户要求发布测试通过后一起研究同化，本轮尚未实施同化。

> **2026-09-24 较早研究恢复点（发布前）**：用户当前研究方向已转为从 sphaera 谱出发的
> 数月至半年气象预报，而非仅继续下文旧的冰川任务。
> 先读 [谱定义与原生 frame](docs/sphaera-seasonal-spectrum-theory.md) 及
> [奇偶阶与长期结构](docs/sphaera-parity-and-long-memory.md)。
> 已完成两轮有限试算：`experiments/sphaera-frame/runs/run-001` 与
> `experiments/sphaera-parity-dynamics/runs/run-001`。
> 前者 456 项检查、283 次原生求值；后者 305 项检查、42 次原生求值。
> 关键发现：原谱消去奇阶；新增奇观测能区分一阶、三阶；去一阶可保留特定形状
> 不变量，却也可能丢失控制未来相位的守恒量。`i` 在奇偶对与二阶内部相位对上
> 的解释必须分开。真实气象训练尚未开始，区域、变量和平均窗口尚未选定。
> 下文保留旧研究的历史交接内容，不作为当前默认执行任务。

写给**下一个接手的人或 agent**。目标不是复述做过什么，而是让你在读完之后
**能直接干活、并且不去重走已经走过的死路**。

---

## 一、先读这一节：诚实的期望

**这份工作的目标**：青藏高原的冰川垮塌与冰湖溃决风险分析。

**已经建立的、你必须先接受的三个结论**（都来自本仓库的实测，不是意见）：

1. **这类事件的精确率被基率封顶**（`xue.derived.discrimination-ceiling.v0`）。
   精确率 = p/a，其中 p 是基率、a 是报警率。**大气物理不进入这一行。**
   我们已经实测的三个探测器报警率都在基率的数千倍上 —— 误报是算术后果，
   不是特征的毛病。**所以「找一个更好的特征」不会修好它。**

2. **三个探测器中，两个是结构性失明的，不是精度不够**：
   - ITS_LIVE 测**冰面流速**，对**岩体**前兆失明（本次事件的前兆在岩体）
   - PDD 触发器 **91% 的时间都触发** —— 饱和即无分辨力
   - 机场测站在 2000 m 以上**只有 2 对**，所以高海拔代表性误差**量不到**

   > **共同点：限制不在数据量，而在观测所在的位置。**
   > **而位置由观测系统当初的目的决定，不由我们的问题决定。**

3. **因此，不要把「再找一个数据集」当作默认解法。** 先问：
   **那个观测系统当初为什么被部署在那里？** 答案会告诉你它能不能回答我们的问题。

**这份工作能救人的方式，很可能不是「预报垮塌」**，而是**审计别人用来预报的仪器**。
已经有两项这样的产出（见第四节），它们是可以直接给别人用的。

---

## 二、机器怎么跑起来

**这是最要紧的一节。** 团块的采集一旦停止，丢掉的观测**永久补不回来** ——
上游保留极短（实测：`cma` 雷达只有约 2.4 小时；`gfs` 自述 "only the newest is kept"）。

### 采集器（必须常驻）

```sh
cd /Users/mingli/Climate/xue-study
python3 scripts/xue_collect.py --loop 600 --max-mb 40
```

- 每 10 分钟一轮，**perishable 源优先**（cma/mrms/jma/sounding/airport）
- 归档到 `archive/`，索引 append-only，**策略性跳过与出错跳过分开标注**
- 若要开机自启，需用户自己装 LaunchAgent（`~/Library/LaunchAgents/` 在本工作区沙箱外）

### 团块页面

```sh
cd /tmp/xue-upstream && npm_config_cache=/tmp/npm-cache npm run dev -- --host :: --port 5199
cd /Users/mingli/Climate/xue-study && while true; do python3 scripts/block_page.py; sleep 120; done
```

页面在 **http://127.0.0.1:5199/block.html**，三个区块：清单 / 技巧表 / 归档的场。
`--host ::` 是必须的：用 `--host localhost` 在 macOS 上只解析到 `::1`，`127.0.0.1` 打不开。

### 依赖

系统 python3 **没有 numpy**。需要 numpy/xarray/zarr 的脚本要跑：

```sh
export UV_CACHE_DIR=/tmp/uv-cache          # 沙箱不允许写 ~/.cache/uv
uv run --offline --with numpy python scripts/xxx.py
# 或用一个已装好的环境：/tmp/m2env/bin/python（含 numpy/scipy/xarray/zarr/s3fs/geopandas）
```

---

## 三、已经确认的死路（**不要去重试**）

| 死路 | 证据 |
|---|---|
| Harmony 子集服务 | 主机级阶段性阻断（根路径 000），非 URL 形状问题 —— 8 次尝试、变量数 1/2/3、GET/POST 全重置 |
| OPeNDAP 取 MERRA-2 | 已退役（`.dds` 返回 `410 Gone`） |
| MERRA-2 日统计产品答 850 hPa 问题 | 只有 8 个变量，**全是地面量**，含 "850" 的变量 0 个 |
| 用卫星振幅找本次事件前兆 | 检测限约 **1 dB / 两月**，而前兆是 **~10 mm/月**，低一个数量级 |
| 用 ITS_LIVE 找前兆 | 已验证：2021–2023 该处流速是基线的 **0.67–0.73**，是**变慢** |
| 从 ITS_LIVE 影像对取季节基线 | `date_dt` 中位 **264 天**；选不选短对会让季节形态**翻转**（峰 3 月 vs 峰 10 月） |
| Zenodo / archive.org / eos.org / NASA COOLR | 本会话内阶段性不可达或 403 |
| GLIMS 页面的 RGI 下载 | 页面**没有直接下载链接**，只有 DOI 与 Zenodo |
| 把 Zarr 存储当文件 GET | 必然 404。键名是 `<节点>/c/<索引…>`，`c` 是独立路径段 |
| 把 poster 当普通栅格读 | poster 是**垂直差分编码**的，须 `cumsum(axis=0) % 256` 反滤波，否则画出的是差分 |

---

## 四、可以交给别人的产出（**最可能真正救人**）

### 1. 高原 850 hPa θe 是**约定，不是观测**

三个再分析系统对同一个问题的三种做法：

```
ERA5      ~331.6 K（63 年九月中位）   给值，贴着地面
GFS/xue   366.2–371.3 K              给值，按递减率外推
MERRA-2   屏蔽 65.1%                  拒绝作答（屏蔽位置与「PS<850」吻合 97.3%）
```

地形三家一致（579.7 / 582.6 / 582.9 hPa），暖池三家一致（差 <1.4 K）。
**所以分歧不在物理，在对一个观测上没有定义的量各自选定的约定。**

**任何建立在高原 850 hPa 上的风险指数都继承这个约定。** 这是可以直接引用的结论。

### 2. 判决取决于误差标准

同一次比对、同一个观测，一致性可以是 **36.2% 或 94.5%**，取决于把什么算作误差。
实测分解（总量 2.14 K）：

```
代表性   0.96 K（下限）   20% 的方差
仪器     0.50 K            5%
模式     1.85 K           75%
```

**任何运行阈值都必须写明它用的是哪一个误差标准**，否则阈值不可比。

---

## 五、还没做、且**不依赖外部数据**的（接手后从这里开始）

1. **非事件侧与特征表**（`(1b)` 的剩余部分）。
   事件侧已通：**事件 = 带不确定度的观测进入系统**（`docs/event-stream.jsonl`，32,222 条，
   误差必填且来源具名）。非事件侧仍缺一个能定义「一条冰川」的边界数据。
   **绕开 RGI 的办法**：用 ITS_LIVE 自己的 `landice` 掩膜做连通域（本仓库已做过，
   朗唐邻域 564 像元 → 3–4 个连通体），**标明它是代理而非权威边界**。

2. **接入更多带误差的观测源**：sounding、cma、mrms 尚未进事件流，
   它们各有可计算或可声明的误差（例如雷达拼图的空间覆盖本身就是个可量化的量）。

3. **把团块里的运行数攒起来**：目前只有 **1 次** gfs 运行，
   所以「跨运行汇总技巧曲线」**并没有真的发生**（曲线变宽靠的是观测累积）。
   等运行多了再做，并且**不要把「样本变多」记成「方法变好」**。

4. **审查 `archive/` 的存储增长**：现有策略约 30 MB/轮。
   若长期跑，需要估算配额并调整 `--max-mb` 与 `FULL_COLLECTIONS`。

---

## 六、这个仓库的纪律（**不要破坏**）

- `docs/claims.toml`：**每条 claim 必须 11 个字段**
  （`claim_id` `canonical_name` `code_symbol` `dimension` `status` `scope`
  `assumptions` `dependencies` `proof_or_certificate` `counterexample_boundary`
  `forbidden_conflations`）。写入前**必须用 `tomllib` 校验**，
  写坏了本会话发生过三次。改动后跑 `scripts/repin_contract.py`。
- `docs/xue-verification-report.md` 与 `docs/maintenance/*`：**append-only**。
  发现前面的结论错了，**追加一条更正**，不要静默改掉。
- `scripts/methodology_check.py`：7 项自检，改动后必须仍 7/7。
- **区分 `RECORDED` 与 `OBSERVED HERE`**：从论文读来的数不是本仓库的测量。
- **区分三类不确定度**：`declared` / `computed` / `observed-scatter`（受污染，混了模式与代表性误差）。
- **区分「未建立」与「无技巧」**：样本不足不是结论。

### 本会话反复踩的坑，写在这里省你时间

1. **读取器已经解码了，不要再套一遍。** GRIB（GDAL 返回已是 °C，又减 273.15）
   和 Zarr（xarray 已应用 `scale_factor`/`add_offset`，又套一遍）各栽一次，
   症状都是「结果差一个巨大常数」。**变换前先看 `.encoding` / `GRIB_UNIT`。**
2. **「结果像噪声」时，先怀疑「我读的是不是那个量」**，再怀疑布局和配色。
   poster 那次就是画的差分而非值。
3. **两侧返回完全相同的字节数 = 这个检查不会失败。** Vite 的 SPA 回退页
   让已删除的路径返回 200 且字节数恒为 57912 —— 本会话被它骗过两次。
4. **进程表不可靠**：`pgrep`/`ps | grep` 会匹配到包装命令，
   给出假的「在跑」或「没跑」。用受管任务的作业列表。
5. **一个不会失败的控制不是控制。** 平坦的曲线同时兼容两个相反的机制；
   三国再分析互比发现不了它们共享的原料误差。

---

## 七、2026-09-23 补充：热穹顶与「退出方案」

### 已完成并提交：高原热穹顶在**地面之上**重做（`d05488c`）

**先解决仪器问题**（本仓库实测：高原地面气压中位 582.6 hPa）：

```
850 hPa   在地下（67.9% 的格点）  ← 「热穹顶」是地下外推值削平到编码上限的产物
500 hPa   在地面之上              ← 可用
250 hPa   归档中最高可用层次（xue 无 200）
```

**结果 —— 跨模式分歧在 500 hPa 上消失：**

```
500 hPa 温度 °C      高原      暖池      中欧
aifs                -4.2     -4.2    -14.3
ecmwf               -3.5     -4.0    -14.0
gfs                 -3.8     -4.7    -14.3
跨模式极差           2.0 K    1.0 K    1.5 K
```

> **高原不是模式天然分歧的地方。只要问一个确实存在的层次，三家就一致。**
> 850 hPa 的 35–40 K 分歧完全来自「向一个在地下的层次要值」。

**而真信号在**：500 hPa 上高原（−3.8～−4.2 °C）与暖池（−4.0～−4.7 °C）几乎一样暖，
中欧同层次 −14 °C。**这是真实的抬升热源信号，不需要任何假象就能看到。**

### 断点：退出方案未完成（**`scripts/sah_exit.py`**）

研究的是**南亚高压**（夏季盘踞高原的行星尺度反气旋），经典层次 100–200 hPa。

**已跑通的部分：**

```
等值线   11316 gpm（按实测 p75 定）
事件     111 次，「覆盖 ≥50% 且持续 ≥3 天」，1979–2021 的 JJA 每日 00Z
持续     中位 5 天，最长 25 天
```

**坏掉的部分：退出分类全部 `undefined`。**

- 症状：`dpos` 非有限 → 质心 `clat`/`clon` 为 NaN
- **未定位**。`above` / `wsum` / `clat` 三行读起来是对的
- **建议的下一步**（不要再读代码猜）：在质心算完之后，直接
  `print(clat[a:b+1], clon[a:b+1])` 打出前三个事件，**看是哪一天变 NaN**
- 也可能是 `np.percentile` 遇 NaN：先试 `np.nanpercentile`

**环境：**

```sh
export UV_CACHE_DIR=/tmp/uv-cache        # /tmp/m2env 与 uv 缓存都已被清过，需联网重取
uv run --quiet --with numpy --with xarray --with zarr --with fsspec --with aiohttp \
  python scripts/sah_exit.py --first 1979 --last 2021 --percentile 75 --json /tmp/sah.json
```

**ERA5 源**（匿名，无需凭据）：

```
https://storage.googleapis.com/gcp-public-data-arco-era5/ar/
  1959-2022-6h-240x121_equiangular_with_poles_conservative.zarr
层次 50 100 150 200 250 300 400 500 600 700 850 925 1000   ← 100 与 200 都在
时间 1959-01-02 .. 2021-12-31，6 小时；经度 0–360
```

**注意读取会超时**：分块是 8 个时次 × 全层次 × 全空间（约 1.2 MB），
读 JJA 每日 00Z 约 600 MB。慢，且会断。必要时分年跑、加 `--retry`。

### 新增的死路（**不要重试**）

| 死路 | 证据 |
|---|---|
| 用文献的 12500 gpm 等值线定 SAH | **该等值线从未进入高原框**（实测最大 11974 gpm）。结果是「零事件」，看起来像「不存在这种事件」，实际是阈值错 |
| 用 `(lon>=w) \| (lon<=e)` 做经度掩膜 | 这是**并集**，任何经度都满足。只在框**跨越 0/360 接缝**时才该用。第一版选到全部 240 格 |

### 新增的两条教训

1. **守卫只挡一个方向等于没有守卫。** 掩膜守卫只拒绝了「选了零格」，
   **没拒绝「选了全部格」** —— 而后者正是那次实际的 bug。
   **任何「选择」都要同时设上下界，并打印选取比例。**
2. **`str.replace` 必须带断言。** 一次替换因为引号不同（`'--json'` vs `"--json"`）
   **静默什么都没做**，直到程序报 `unrecognized arguments` 才暴露。
   同一段代码里第一处替换有断言就报了错，第二处没有就无声失败。

### 第三次同一类解码错误

脚本写 `q.get("unit","K")`，但 `q` 是**量化字典**，**没有 `unit` 键**，
于是默认成 K，对摄氏值又减 273.15，得出 −273 °C。

**单位在 *variable* 字典里，不在 *quantization* 里。**

> 与 GRIB（GDAL 返回已是 °C，又减 273.15）、Zarr（xarray 已解码，又套量化）同类。
> **判据：数值荒谬（−273、1e8、−71）时，先查单位与解码，不要查物理。**
